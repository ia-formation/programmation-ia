*name
censure_voyelles_boucle
*in plain str
bonjour
censure voyelles
aaaaahhhhhh
\*****
balivernes
 : Make sure your code works for the empty string!
\*a*b*c*
12345
*in secret_plain str
?!?!
abcdefghijklmnopqrstuvwyxz
a
aaaaaaa
*code
def censure_voyelles_boucle(word):
    out_str = ""
    i = 0
    while i < len(word):
        char = word[i]
        if char == "a" or char == "e" or char == "i" or char == "o" or char == "u":
            out_str += "*"
        else:
            out_str += char
        i += 1
    return out_str
